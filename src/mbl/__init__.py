"""Model-based learning for stochastic control.

The import root for the whole modern tree. `src/lqr/` is deliberately NOT part
of this package: it is frozen legacy, excluded from packaging, linting and
type-checking, and scheduled for deletion once the modern tree fully replaces
it.
"""
