"""The neural policy family: a plain network mapping observation to control,
carried as the learned-from-scratch reference the structured families are
measured against.

Re-export-free by the same rule as `mbl.models` — see that package's docstring
for why the file exists at all.
"""
