"""Control constraints (Tier 1).

`constraint.py` declares the structural `Constraint` protocol; `box_constraint.py`
is the box this project's constrained studies project onto, dual-backend.

Re-export-free, under `core`'s purity pin — see `mbl.core.cost` and `mbl.models`.
"""
